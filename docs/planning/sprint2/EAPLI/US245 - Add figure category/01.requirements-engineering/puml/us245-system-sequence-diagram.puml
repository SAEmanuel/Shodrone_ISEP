@startuml
skinparam monochrome true
skinparam packageStyle rectangle
skinparam shadowing false

title System Sequence Diagram (SSD) - Add Figure Category

autonumber

actor "Show Designer" as SD
participant ":System" as System

activate SD

SD -> System: requestAddCategory(name, description)
activate System

System -> System: validateCategoryName(name)
System -> System: checkCategoryUniqueness(name)
System -> System: validateDescription(description)
alt Name invalid, duplicate, or description invalid
    System --> SD: errorMessage("Invalid or duplicate category name, or invalid description")
else Name and description valid and unique
    System -> System: saveCategory(id, name, description, status, cretedOn, createdBy)
    System --> SD: confirmationMessage("Category added successfully")
end

deactivate System
deactivate SD

@enduml
