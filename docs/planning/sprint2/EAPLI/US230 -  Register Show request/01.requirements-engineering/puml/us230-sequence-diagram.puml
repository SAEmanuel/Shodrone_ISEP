```
@startuml
actor "CRM Collaborator" as Collaborator
participant ":System" as System

Collaborator -> System: login()
System -> Collaborator: authenticated
Collaborator -> System: registerShowRequest(customer, place, time, numDrones, duration, figures)
System -> System: validateCustomer(customer)
alt customer is active or VIP
    System -> System: validateFigures(figures, customer)
    alt figures are valid
        System -> System: createShowRequest(customer, numDrones, duration)
        System -> System: createShowDescription(place, time, figures)
        System -> System: setShowRequestAuthor(collaborator)
        System -> System: addShowRequestStatus("Created", timestamp)
        System -> System: persistShowRequest()
        System -> Collaborator: "Show request registered successfully with ID SR-001"
    else figures invalid
        System -> Collaborator: error("Invalid figures: must be public or exclusive")
    end
else customer invalid
    System -> Collaborator: error("Only active or VIP customers allowed")
end

@enduml
```